[
	{'hooxy', [
		%% user-defined hook to provide domain to IP translation
		{hook, {erlang_hook, hooxy_test_hook}},
		%% the port this proxy will listen on
		{proxy_port, 8080},
		%% the default TTL for cache entries (1 minute)
		{ttl, 60}
	]}
].
